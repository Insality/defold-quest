local deftest = require("deftest.deftest")

function init(self)
	deftest.add(require("test.test_quest"))
	deftest.add(require("test.test_quest_token"))

	local is_report = (sys.get_config("test.report") == "1")
	deftest.run({ coverage = { enabled = is_report } })
end
